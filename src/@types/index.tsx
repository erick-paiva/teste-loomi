export interface User {
  userName: string;
  name: string;
  avatar: string;
}

export interface IProduct{
  createdAt: string;
  name: string;
  color: string;
  status: string;
  description: string;
  id: string;
}